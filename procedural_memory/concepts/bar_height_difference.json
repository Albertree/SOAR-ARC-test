{
  "concept_id": "bar_height_difference",
  "version": 1,
  "description": "Bars at odd columns; add color-5 bar with height = |sum(heights_A) - sum(heights_B)| at next empty odd column",
  "signature": {
    "grid_size_preserved": true,
    "requires_content_diff": true
  },
  "parameters": {
    "bg": {"type": "color", "infer": "bg_color"}
  },
  "steps": [
    {
      "id": "s1",
      "primitive": "bar_height_difference",
      "args": {"grid": "$input", "bg": "$bg"},
      "output": "result"
    }
  ],
  "result": "$result"
}
