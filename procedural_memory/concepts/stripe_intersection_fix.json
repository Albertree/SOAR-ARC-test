{
  "concept_id": "stripe_intersection_fix",
  "version": 1,
  "description": "Two stripes (one H band, one V band) cross on a bg grid. At the intersection the pure stripe's color dominates; fix it to show the impure stripe's color.",
  "signature": {
    "grid_size_preserved": true,
    "color_preserved": true,
    "requires_content_diff": true
  },
  "parameters": {},
  "steps": [
    {"id": "s1", "primitive": "stripe_intersection_fix", "args": {"grid": "$input"}, "output": "result"}
  ],
  "result": "$result"
}
