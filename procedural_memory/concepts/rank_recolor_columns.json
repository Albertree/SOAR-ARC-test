{
  "concept_id": "rank_recolor_columns",
  "version": 1,
  "description": "Recolor columns of a single non-bg color by rank of their height (tallest=1, next=2, etc.)",
  "signature": {
    "grid_size_preserved": true,
    "size_ratio": null,
    "color_preserved": false,
    "requires_content_diff": true,
    "input_constraints": []
  },
  "parameters": {
    "target_color": {"type": "color", "infer": "non_bg_single"},
    "bg": {"type": "color", "infer": "bg_color"}
  },
  "steps": [
    {"id": "s1", "primitive": "rank_recolor_columns", "args": {"grid": "$input", "target_color": "$target_color", "bg": "$bg"}, "output": "result"}
  ],
  "result": "$result"
}
