{
  "concept_id": "recolor_shapes_by_template",
  "version": 1,
  "description": "Header templates behind L-shaped separator; body shapes of color 3 get recolored to matching template color (rotation/reflection invariant)",
  "signature": {
    "grid_size_preserved": true,
    "requires_content_diff": true
  },
  "parameters": {
    "bg": {"type": "color", "infer": "bg_color"}
  },
  "steps": [
    {
      "id": "s1",
      "primitive": "recolor_shapes_by_template",
      "args": {"grid": "$input", "bg": "$bg"},
      "output": "result"
    }
  ],
  "result": "$result"
}
