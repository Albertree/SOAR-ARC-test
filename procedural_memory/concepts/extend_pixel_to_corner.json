{
  "concept_id": "extend_pixel_to_corner",
  "version": 1,
  "description": "Each non-bg pixel draws an L-shaped line toward the nearest grid corner",
  "signature": {
    "grid_size_preserved": true,
    "requires_content_diff": true
  },
  "parameters": {
    "bg": {"type": "int", "infer": "bg_color"}
  },
  "steps": [
    {
      "id": "s1",
      "primitive": "extend_pixel_to_corner",
      "args": {"grid": "$input", "bg": "$bg"},
      "output": "result"
    }
  ],
  "result": "$result"
}
