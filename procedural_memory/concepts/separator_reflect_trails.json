{
  "concept_id": "separator_reflect_trails",
  "version": 1,
  "description": "Reflect colored markers across a horizontal separator row (all 9s). Below the separator, markers (2) follow chains of trail cells (6) to new positions. Above the separator, reflected markers (5) appear at mirrored positions.",
  "signature": {
    "grid_size_preserved": true,
    "color_preserved": false,
    "requires_content_diff": true
  },
  "parameters": {
    "bg": {"type": "color", "infer": "bg_color"}
  },
  "steps": [
    {"id": "s1", "primitive": "separator_reflect_trails", "args": {"grid": "$input", "bg": "$bg"}, "output": "result"}
  ],
  "result": "$result"
}
