{
  "concept_id": "reverse_rings",
  "version": 1,
  "description": "Reverse the color order of concentric rectangular rings so outermost becomes innermost.",
  "signature": {
    "grid_size_preserved": true,
    "color_preserved": true,
    "requires_content_diff": true
  },
  "parameters": {},
  "steps": [
    {"id": "s1", "primitive": "reverse_rings", "args": {"grid": "$input"}, "output": "result"}
  ],
  "result": "$result"
}
