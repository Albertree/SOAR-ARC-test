{
  "concept_id": "relocate_cross_template",
  "version": 1,
  "description": "Find cross templates with connector+markers, match to isolated anchor dots, transform and redraw at anchor positions",
  "signature": {
    "grid_size_preserved": true,
    "requires_content_diff": true
  },
  "parameters": {
    "bg": {"type": "color", "infer": "bg_color"}
  },
  "steps": [
    {
      "id": "s1",
      "primitive": "relocate_cross_template",
      "args": {
        "grid": "$input",
        "bg": "$bg"
      },
      "output": "result"
    }
  ],
  "result": "$result"
}
